<!-- Template for every abstention, filled with no model call so the wording never
varies with the reason: a differentiated refusal is a disclosure. Must open with the
exact phrase "I do not have approved validation data", which system.md and
packages/eval/checks.py both pin. No square brackets anywhere. The {subject} slot is
filled by the composer; its fallback wording is DEFAULT_ABSTAIN_SUBJECT in
packages/composer. Matches the q-unknown-antibody golden ideal.

No em dash in the served text. This template ships identically on every abstention, so
one here is not a tendency the model might resist, it is a guaranteed tell in a fixed
share of replies. max_em_dashes on the abstain block is what keeps it out. -->
I do not have approved validation data for {subject}, so I cannot tell you how well
validated it is.

That absence is not evidence it performs poorly. It means I have nothing to report, which
is a different thing. If it would help, I can walk through what evidence would establish
confidence for the application you have in mind, so you know what to look for in a
datasheet or ask a vendor for.
