# Copy to .env (gitignored), fill in, then load into the shell:
#   set -a; source .env; set +a
# The key lives in the environment only, never in a tracked file.

OPENAI_API_KEY=
ABBIE_MODEL=gpt-5-mini
